require('nvim-tree').setup({
    view = {
        mappings = {
            list = {
                --{ key = "<CR>", action=""},

                --{ key = "<CR>", action="edit_in_place", mode="n"},
            }
        }

    }
})
